<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet" />
    <title>Duty Call | Login</title>   
</head>

<!-- style="background-image: url(public\stylesheets\images\leaves.jpg)" -->


<body class="bg-gray-900  flex flex-col justify-center items-center m-12">
    <h1 class="font-bold text-3xl text-yellow-600">Sign in to Duty Call</h1>
    <img src="public\stylesheets\images\dutyCALL.PNG" class="w-40" />

    <div class="bg-gray-800 py-12 px-12 flex flex-col items-center border-b-2 border-l-2 border-gray-700 rounded-lg ">        
        
            <form id="login-form" class="max-w-xl w-full text-gray-200 mt-4 px-8 md:px-0">
                <div class="flex flex-col md:flex-row md:-mx-2">
                    <label class="sr-only" for='email'>Email</label>
                    <!-- name = email will connect name to server -->
                    <input
                        class="bg-gray-700 mt-4 border border-gray-600 rounded-lg py-2 px-4 w-full md:mx-2 placeholder-yellow-200"
                        placeholder="exampemail@email.com" type='email' id='email' name='email' required>
            
                    <label class="sr-only" for='name'>Password</label>
                    <!-- name = name will connect name to server -->
                    <input
                        class="bg-gray-700 mt-4 border border-gray-600 rounded-lg py-2 px-4 w-full md:mx-2 placeholder-yellow-200"
                        placeholder="Password" type='password' id='password' name='password' required>
                </div>
            
                <div class="flex md:flex-row md:-mx-2">
                    <button class="hover:text-green-300 text-sm mt-4 rounded-lg py-2 px-4 w-full md:mx-2"
                        type='submit'>Login</button>
            
                    <!-- use anchor to connect back to login page -->
                    <a class="hover:text-green-300 text-sm mt-4 rounded-lg py-2 px-4 w-full md:mx-2" href='/register'>Register</a>
                </div>
            </form>

    </div>
<script src="/public/javascript/login.js"></script>
</body>
</html>